# workspaces

Shared surfaces that span REST / MCP / GraphQL shell chrome.

- `ComingSoonWorkspace` — full-page placeholder for MCP and GraphQL. Shown in-place under
  the header (no sidebar, no tab strip, no new tab) via `AppContent` workbench state.
