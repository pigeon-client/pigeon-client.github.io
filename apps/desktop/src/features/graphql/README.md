# graphql

Status: **stub**. The GraphQL workspace tab exists (`⌘⇧G`, singleton OS window like REST/MCP)
but renders only `GraphqlComingSoon` — no query editor, schema introspection, or send path yet.

## Public API (`index.ts`)
- `GraphqlComingSoon`

## Extend
When building this out for real, follow the standard feature skeleton used by `rest/*` and
`mcp`: `components/ hooks/ lib/ services/ store.ts types.ts index.ts README.md`.
