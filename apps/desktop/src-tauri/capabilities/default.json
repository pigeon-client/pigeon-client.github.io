{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["main", "mcp", "graphql"],
  "permissions": [
    "core:default",
    "updater:default",
    "process:allow-restart",
    "opener:default"
  ]
}
